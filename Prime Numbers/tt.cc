#include <bits/stdc++.h>

#define DEBUG

using ll = long long;
ll constexpr INF = 1e18;

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    for (ll i = 0; i < 1e9; ++i) {
        if (i % ll(1e10) == 0) {
            std::cout << "Hello, World" << '\n';
        }
    }
}
